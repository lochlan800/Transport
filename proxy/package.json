{
  "name": "england-transport-proxy",
  "version": "1.0.0",
  "description": "Free proxy for England Transport Finder — serves real train & bus departures from TransportAPI",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "deploy": "wrangler deploy"
  },
  "engines": { "node": ">=18" },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
